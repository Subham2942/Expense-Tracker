# Expense Tracker Frontend

A minimal Expo SDK 54 React Native application using Expo Router.

## Run the app

```bash
npm install
npm start
```

You can also run a specific platform:

```bash
npm run android
npm run ios
npm run web
```

Start building from `app/index.tsx`. Add another file inside `app` when you need a new screen.
